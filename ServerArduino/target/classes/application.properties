# Spring Data JPA / PostgreSQL settings
spring.datasource.url=jdbc:postgresql://localhost:5432/climate_db
spring.datasource.username=postgres
spring.datasource.password=123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port placeholder. The user indicated an arbitrary port; change if needed.
server.port=6565

# JSP settings
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
